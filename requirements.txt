# ── Core web (Flask UI on :5000, FastAPI service on :8000) ────────────────
Flask==3.1.3
python-dotenv==1.2.2
fastapi==0.139.2
uvicorn[standard]==0.51.0
pydantic==2.13.4

# ── Data / ML stack ───────────────────────────────────────────────────────
pandas==3.0.3
numpy==1.26.2
scikit-learn==1.9.0
tensorflow==2.15.0
xgboost==3.2.0
statsmodels==0.14.6
prophet==1.3.0
scipy==1.16.3
shap==0.51.0

# ── Market data (was imported everywhere but missing from this file) ──────
yfinance==1.5.1
requests==2.34.2

# ── Plotting / experiments ────────────────────────────────────────────────
matplotlib==3.11.0

# ── stock_predictor.py (Streamlit variant) + ops dashboard ────────────────
streamlit>=1.40
plotly>=5.24
vaderSentiment>=3.3.2

# ── Production ops (Day 9): shared price cache + experiment tracking ──────
redis==5.2.1
mlflow==2.22.0

# ── Testing ───────────────────────────────────────────────────────────────
httpx==0.28.1
fakeredis==2.26.2       # Redis-layer tests without a running server
