# Slim dependency set for the Docker serving image (API + dashboard).
#
# Deliberately EXCLUDES tensorflow (~600 MB) and prophet: the serving champion
# is ARIMA and the registry imports TF lazily, so /predict and /backtest for
# every non-deep model work without it. The lstm/patchtst registry entries
# raise a clear ImportError inside the container — documented, not hidden.
# Full research environment: requirements.txt.
fastapi==0.139.2
uvicorn[standard]==0.51.0
pydantic==2.13.4
Flask==3.1.3            # historical.py (indicator math reused by /indicators)
pandas==3.0.3
numpy==1.26.2
scipy==1.16.3
scikit-learn==1.9.0
statsmodels==0.14.6
# xgboost==3.2.0 — installed by the Dockerfile with --no-deps: the Linux
# wheel's dependency list drags in nvidia-nccl-cu12 (~300 MB GPU runtime)
# that a CPU container never uses. Listed here for humans, pinned there.
yfinance==1.5.1
requests==2.34.2
matplotlib==3.11.0      # MLflow equity-curve artifacts
redis==5.2.1
mlflow-skinny==2.22.0   # tracking client only; `mlflow ui` runs on the host
streamlit>=1.40
plotly>=5.24
